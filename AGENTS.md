# quant-risk-monitor

Portfolio risk rule checker.

## Commands

```bash
pip install -e ".[dev]"
quant-risk check --config configs/default.yaml --out state/alerts.json
pytest -q
ruff check src tests
```
